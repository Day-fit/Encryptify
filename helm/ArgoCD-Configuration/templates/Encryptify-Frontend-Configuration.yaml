apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: Encryptify-Frontend-Configuration
  namespace: encryptify
spec:
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
  project: encryptify
  source:
    chart: Encryptify-Frontend
    repoURL: https://github.com/Day-fit/Encryptify-CD.git
    helm:
      releaseName: Encryptify-Frontend
  destination:
    server: "https://kubernetes.default.svc"
    namespace: encryptify