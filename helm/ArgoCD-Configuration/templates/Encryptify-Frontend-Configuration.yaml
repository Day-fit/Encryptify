apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: encryptify-frontend-configuration
  namespace: encryptify
spec:
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
  project: default
  source:
    path: Encryptify-Frontend
    repoURL: https://github.com/Day-fit/Encryptify-CD.git
    targetRevision: HEAD
    helm:
      releaseName: encryptify-frontend
  destination:
    server: "https://kubernetes.default.svc"
    namespace: encryptify