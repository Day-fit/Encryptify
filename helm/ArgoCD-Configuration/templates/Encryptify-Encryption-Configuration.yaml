apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: Encryptify-Encryption-Configuration
  namespace: encryptify
spec:
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
  project: encryptify
  source:
    chart: Encryptify-Encryption
    repoURL: https://github.com/Day-fit/Encryptify-CD.git
    targetRevision: HEAD
    helm:
      releaseName: Encryptify-Encryption
  destination:
    server: "https://kubernetes.default.svc"
    namespace: encryptify