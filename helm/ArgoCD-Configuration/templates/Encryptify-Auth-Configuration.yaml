apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: Encryptify-Auth-Configuration
  namespace: encryptify
spec:
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
  project: encryptify
  source:
    chart: Encryptify-Auth
    repoURL: https://github.com/Day-fit/Encryptify-CD.git
    helm:
      releaseName: Encryptify-Auth
  destination:
    server: "https://kubernetes.default.svc"
    namespace: encryptify