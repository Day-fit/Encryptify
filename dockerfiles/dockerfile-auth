# syntax=docker/dockerfile:1
FROM eclipse-temurin:21.0.7_6-jdk-alpine-3.21

LABEL name="encryptify-auth"
LABEL maintainer="Dayfit"
LABEL description="Encryptify Auth Service"

COPY ./.mvn /app/.mvn
COPY ./mvnw /app/mvnw

COPY ./encryptify-auth /app/encryptify-auth
WORKDIR /app

# Setup maven settings
RUN --mount=type=secret,id=gh_name \
    --mount=type=secret,id=github_token \
    sh -c 'echo \
"<settings>" \
"  <servers>" \
"    <server>" \
"      <id>github</id>" \
"      <username>$(cat /run/secrets/gh_name)</username>" \
"      <password>$(cat /run/secrets/github_token)</password>" \
"    </server>" \
"  </servers>" \
"</settings>" > /tmp/settings.xml'

# Compile source code
RUN \
    chmod +x ./.mvn && \
    chmod +x ./mvnw && \
    cd /app/encryptify-auth && \
    ../mvnw package -DskipTests -s /tmp/settings.xml && \
    mv /app/encryptify-auth/target/encryptify-auth-*.jar /app/encryptify-auth.jar

CMD ["java", "-jar", "/app/encryptify-auth.jar"]

EXPOSE 8083